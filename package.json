{
  "name": "cloudfile-local-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only bridge for the CloudFile Chrome extension.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
